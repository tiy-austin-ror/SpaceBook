A new event was created in <%= link_to @room.name,
        :controller => "rooms",
        :action => "show",
        :only_path => false,
        :campus_id => @room.campus.id,
        :id => @room.id,
        :host => "example.com" %>.

  Name: <%= @event.name %>
  When: <%= @event.start_time %>

To see more details about the event, please visit the <%= link_to "event page",
        :controller => "events",
        :action => "show",
        :only_path => false,
        :campus_id => @room.campus.id,
        :room_id => @event.room.id,
        :id => @event.id,
        :host => "example.com" %>.
